services:
  arangodb:
    image: arangodb:3.12
    container_name: arangodb-proto
    environment:
      ARANGO_NO_AUTH: "1"
    ports:
      - "8529:8529"
    volumes:
      - ./data:/var/lib/arangodb3
