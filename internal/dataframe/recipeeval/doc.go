// Package recipeeval is a storage-neutral reference interpreter for
// differential and conformance tests only. Production server, runtime, and
// materialization paths must consume checked semantic plans and the physical
// compiler; they must not import this map-based evaluator.
package recipeeval
