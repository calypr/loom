schema:
  - internal/graphqlapi/schema/schema.graphqls
  - generated/graphqlapi/schema/fhir_schema.graphqls

exec:
  layout: follow-schema
  dir: generated/graphqlapi/executor
  filename_template: "{name}.generated.go"
  package: executor

model:
  filename: generated/graphqlapi/model/models.go
  package: model

resolver:
  layout: follow-schema
  dir: generated/graphqlapi/resolver
  package: resolver

skip_mod_tidy: true
# The generated JSON scalar unmarshaller needs the post-generation correction
# in cmd/gqlgenfix. Skip gqlgen's pre-generation package validation so that
# correction can be applied before the package is compiled.
skip_validation: true
struct_tag: gqlgen

autobind:
  - github.com/calypr/loom/generated/fhir

models:
  JSON:
    model:
      - encoding/json.RawMessage
